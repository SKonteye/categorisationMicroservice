package www.diti5.springboot1.categorieService.config;

/*
@Configuration
@EnableSwagger2
public class SwaggerConfig
{
    @Bean
    public Docket api()
    {
        return new Docket(DocumentationType.SWAGGER_2)
                .select()
                .apis(RequestHandlerSelectors.basePackage("www.diti5.springboot1.controllers"))
                .paths(PathSelectors.any())
                .build();
    }
}
*/

